package com.roxgps.helper

import android.Manifest
import android.content.pm.PackageManager
import androidx.core.app.ActivityCompat
import com.roxgps.ui.BaseMapActivity

/**
 * Helper class untuk mengelola perizinan (permissions) yang dibutuhkan oleh aplikasi.
 *
 * @param activity Instance dari [BaseMapActivity] yang digunakan untuk memeriksa dan meminta perizinan.
 */
class PermissionHelper(private val activity: BaseMapActivity) {

}